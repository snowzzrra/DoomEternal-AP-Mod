[tool.ruff]
target-version = "py310"
line-length = 120
exclude = [
    "build",
    "release",
    "dist",
    "gameresources",
    "vanillamaps",
    "vanilla_decls",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "PIE", "RUF"]
